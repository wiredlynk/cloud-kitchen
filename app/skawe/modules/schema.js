import login from "~/schema/login";
import register from "~/schema/register";
import forgotPassword from "~/schema/forgot-password";
import menu from "~/schema/menu";
import kitchens from "~/schema/kitchens";
import food from "~/schema/food";
import hotels from "~/schema/hotels";
import settings from "~/schema/settings";

export const schema = {
  login,
  register,
  forgotPassword,
  settings,
  menu,
  kitchens,
  food,
  hotels,
};
